# All paths
/*
    # Block site from being framed with X-Frame-Options
    X-Frame-Options: DENY
    X-XSS-Protection: 1; mode=block

    # Prevent browsers from incorrectly detecting non-scripts as scripts
    X-Content-Type-Options: nosniff

    # only https
    # Strict-Transport-Security: max-age=31536000; includeSubDomains; preload

    # Never send the Referer header
    Referrer-Policy: no-referrer

    # Don't load any resource type not explicitly enabled
    # Disable plugins like Flash or Silverlight
    # Load images, scripts, stylesheets and fonts from self
    # Send reports to report-uri.io
    # Add your link to the end of the line: report-uri 
    Content-Security-Policy: frame-ancestors 'self'; default-src 'self' cloudfront.net *.cloudfront.net; style-src 'self' 'unsafe-inline' fonts.googleapis.com unpkg.com/netlify-cms@%5E2.0.0/dist/cms.css; font-src 'self' fonts.gstatic.com gstatic.com *.gstatic.com fonts.googleapis.com; script-src 'self' 'unsafe-eval' 'unsafe-inline' identity.netlify.com npkg.com/netlify-cms@2.10.192/dist/cms.js unpkg.com/browse/netlify-cms@2.10.192/dist/netlify-cms.js unpkg.com/netlify-cms@%5E2.0.0/dist/cms.js; child-src 'self'; img-src 'self' cloudfront.net *.cloudfront.net blob: data:; object-src 'none'; connect-src 'self' www.netlifystatus.com/api/v2/components.json; base-uri 'none'; form-action 'self';
